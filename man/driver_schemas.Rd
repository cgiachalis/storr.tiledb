% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TileDBDriverSchemas.R
\name{driver_schemas}
\alias{driver_schemas}
\title{TileDB Driver Schemas}
\usage{
driver_schemas(uri = NULL, ctx = NULL, none_filter = FALSE)
}
\arguments{
\item{uri}{Optional URI path to \code{TileDB} driver. If not given, the default
template array schemas will be used.}

\item{ctx}{Optional \link[tiledb:tiledb_ctx]{tiledb_ctx} object.}

\item{none_filter}{\code{TRUE} for no filters, \code{FALSE} for default filters.
Applied on default schemas and not on schemas extracted from a URI path.}
}
\value{
An object of class \link{TileDBDriverSchemas}.
}
\description{
Tune driver performance and storage characteristics: compression algorithms,
compression levels, tile capacity, cell order, and tile order settings.
This is useful for creating storage drivers for use cases that need
different trade-offs (speed vs. compression, memory vs. disk).
}
\details{
Use \code{driver_schemas()} to:
\itemize{
\item Create schemas with optional filters
\item Dynamically apply compression to individual attributes
\item Persist those customizations into the underlying TileDB arrays
}
}
\examples{
ctx <- new_context()
sto_schemas <- driver_schemas(ctx = ctx, none_filter = TRUE)


# Set up a ZSTD filter with high compression
flt <- tiledb::tiledb_filter("ZSTD", ctx = ctx)
flt <- tiledb::tiledb_filter_set_option(flt,"COMPRESSION_LEVEL", 22)
fl_list <- tiledb::tiledb_filter_list(flt, ctx = ctx)

# Apply filter list to 'value' attribute (CAS storage data)
sto_schemas$SchemaData$attr_value <- fl_list

# 'data' schema is modified now
sto_schemas$SchemaData$schema()

}
