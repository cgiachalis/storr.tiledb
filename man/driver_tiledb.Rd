% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/driver_tiledb.R
\name{driver_tiledb}
\alias{driver_tiledb}
\alias{driver_tiledb_create}
\title{TileDB Storr Driver}
\usage{
driver_tiledb(uri, context = NULL, init = FALSE, ...)

driver_tiledb_create(uri, hash_algorithm = NULL, compression_level = -7,
  context = NULL)
}
\arguments{
\item{uri}{The URI path of storr.}

\item{context}{Optional \link[tiledb:tiledb_ctx]{tiledb_ctx} object.}

\item{init}{Should the driver be created if not exist? Default is  \code{FALSE}.}

\item{...}{Other arguments passed to driver's create method when \code{init = TRUE}.
Valid arguments: \code{hash_algorithm}, \code{compression_level} and \code{keep_open}.}

\item{hash_algorithm}{Select a hash algorithm supported by \link[digest:digest]{digest}:
'md5', 'sha1', 'crc32', 'sha256', 'sha512', 'xxhash32', 'xxhash64', 'murmur32', 'spookyhash', 'blake3', 'crc32c', 'xxh3_64', 'xxh3_128'. If not given, the  default is 'md5'.}

\item{compression_level}{Set an integer value for ZSTD compression level.}
}
\value{
\itemize{
\item \emph{driver_tiledb()} returns a \link{TileDBDriver} object.
\item \emph{driver_tiledb_create()} returns logical \code{TRUE} invisibly, for successful
driver creation.
}
}
\description{
Storr driver using TileDB storage engine.
}
\section{Details}{
\code{driver_tiledb()} returns the TileDB driver given a URI path. You can create
a new driver using \code{init} argument or with \code{driver_tiledb_create()}.

Note that \code{\link[=storr_tiledb]{storr_tiledb()}} abstracts the driver creation, so it's the preferred way unless
you want to pass the driver onto  \link[storr:storr]{storr}.
}

\section{Class Methods Summary}{
For complete definitions, see \strong{Methods} section in \link{TileDBDriver} and its
parent class \link{CAS}.

\strong{Active Fields}
\itemize{
\item \strong{\code{hash_algorithm}} - Property for managing the hash algorithm (read/write)
\item \strong{\code{members_instantiated}} - Property indicating instantiation status (read-only)
\item \strong{\code{size}} - Get storr size (read-only)
}

\strong{Lifecycle}
\itemize{
\item \strong{\code{open()}} - Opens the driver for read/write operations
\item \strong{\code{create()}} - Create a new driver object
\item \strong{\code{close()}} - Closes the driver
\item \strong{\code{destroy()}} - Destroy/delete the driver
}

\strong{Hash Management}
\itemize{
\item \strong{\code{get_hash()}} and \strong{\code{mget_hash()}} - Retrieve hash values for a given key and namespace
\item \strong{\code{set_hash()}} and \strong{\code{mset_hash()}} - Set hash values with metadata like expiry date-times and notes
\item \strong{\code{exists_hash()}} - Verify the existence of specific keys, namespaces
\item \strong{\code{del_hash()}} - Remove key-namespace pairs
\item \strong{\code{delete_namespaces()}} - Clear namespaces or delete specified ones
\item \strong{\code{list_namespaces()}}, \strong{\code{list_keys()}} - Retrieve all namespaces or keys for a given namespace
}

\strong{Object Management}
\itemize{
\item \strong{\code{get_object()}} and \strong{\code{mget_object()}} - Fetch serialized R objects using hash values
\item \strong{\code{set_object()}} and \strong{\code{mset_object()}} - Store serialized R objects
\item \strong{\code{exists_object()}} - Verify the existence of specific objects
\item \strong{\code{del_object()}} - Remove serialized objects.
\item \strong{\code{delete_unused_hashes()}} - Remove hashes that are not in active use
\item \strong{\code{list_hashes()}} - Retrieve all hashes
\item \strong{\code{list_unused_hashes()}} - Identify unused hashes
}

\strong{Key-Namespace Metadata}
\itemize{
\item \strong{\code{get_keymeta()}}, \strong{\code{set_keymeta()}}, and \strong{\code{mset_keymeta()}} - Manage metadata such as
expiry times and notes for key-namespace pairs
}

\strong{Expiration Management}
\itemize{
\item \strong{\code{keys_with_expiration}} and \strong{\code{keys_without_expiration}} - Retrieve the key namespace pairs with
or without expiration timestamps
\item \strong{\code{expired_keys()}} and \strong{\code{unexpired_keys()}} - Retrieve the (un)expired key namespace pairs
\item \strong{\code{delete_expired_keys()}} - Delete all expired keys or for specific namespaces
\item \strong{\code{num_expired_keys()}} and \strong{\code{num_unexpired_keys()}} - Get the number of (un)expired keys or
for specific namespaces
\item \strong{\code{has_expired_keys()}} and \strong{\code{has_unexpired_keys()}} - Verify the existence of (un)expired keys or
for specific namespaces
}

\strong{Export Utilities}
\itemize{
\item \strong{\code{export_tdb()}} - Export objects to another TileDB storr
}

\strong{Directory Methods}
\itemize{
\item \strong{\code{dir_tree()}} - Displays directory structure
\item \strong{\code{dump()}} - Outputs a string representation of storr structure
}
}

\section{Data Model}{
The underlying structure is similar to \link[storr:driver_dbi]{driver_dbi()};
a content-addressed database that consists of two (array) tables.

Specifically, the driver storage is a TileDB Group with two member Arrays
which are stored relative to Group's URI path. The next sub-sections describe
the group structure and array data models.

\strong{Group}

\emph{Members}
\enumerate{
\item \code{tbl_keys} (array): maps key-namespace pairs to hashes (and to expiry and/or
notes, optional)
\item \code{tbl_data} (array): maps hashes to values (serialised R objects)
}

\emph{Metadata}
\itemize{
\item \code{hash_algo}: The name of hash algorithm.
\item \code{type}: Group identifier, \code{"storr"}
}

\strong{Arrays}

\emph{tbl_keys} - A 2D sparse array that maps key-namespace pairs to hashes and key-metadata.
\itemize{
\item Dimensions: \code{namespace} (\emph{ASCII}) and \code{key} (\emph{ASCII})
\item Attributes: \code{hash} (\emph{ASCII}), \code{expires_at} (\emph{DATETIME_MS}) and \code{notes} (\emph{UTF8})
}

\emph{tbl_data} - A 1D sparse array that maps hashes to object values.
\itemize{
\item Dimensions: \code{hash} (\emph{ASCII})
\item Attributes: \code{value} (\emph{ASCII})
}

TileDB datatypes in parentheses.
}

\examples{
\dontrun{
# URI path
uri <- tempfile()

# create driver
dr <- driver_tiledb(uri, init = TRUE)

dr$print()
# R6Class: <TileDBDriver>
#  → URI Basename: file6bb0182c1362
#   • Arrays: "tbl_keys" and "tbl_data"

# members
dr$names()
# "tbl_keys" "tbl_data"
}

}
\seealso{
\code{\link[=storr_tiledb]{storr_tiledb()}}
}
